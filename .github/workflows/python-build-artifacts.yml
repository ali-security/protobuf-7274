name: Python Build Artifacts

on:
  push:
    branches: [ "*" ]
  workflow_dispatch:

env:
    PIP_INDEX_URL: "https://:2022-09-14T00:00:00.000000Z@time-machines-pypi.sealsecurity.io/"

jobs:
#   maynlinux:
#     name: Build Python artifacts on ${{ matrix.os }} - ${{ matrix.image }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - os: ubuntu-latest
#             script: kokoro/release/python/linux/build_artifacts.sh
#             image: quay.io/pypa/manylinux2010_x86_64
#             image_tag: 2021-09-27-ed30de0
#           - os: ubuntu-latest
#             script: kokoro/release/python/linux/build_artifacts.sh
#             image: quay.io/pypa/manylinux2014_x86_64
#             image_tag: 2021-09-27-ed30de0
#           - os: ubuntu-latest
#             script: kokoro/release/python/linux/build_artifacts.sh
#             image: quay.io/pypa/manylinux_2_24_x86_64
#             image_tag: 2021-09-27-ed30de0
#         #   - os: macos-latest
#         #     script: kokoro/release/python/macos/build_artifacts.sh
#         #     shell: bash
#         #   - os: windows-latest
#         #     script: kokoro/release/python/windows/build_artifacts.bat
#         #     shell: cmd

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: MatteoH2O1999/setup-python@v4
#         with:
#           python-version: '3.6' # Only used to run the build script
    
#       - name: Run build script (Bash)
#         run: |
#             $PYTHON_EXE -m venv .venv
#             source .venv/bin/activate
#             python -m pip install -U virtualenv
#             bash ${{ matrix.script }}
#         env:
#             PYTHON_EXE: python3
#             DOCKER_IMAGE: ${{ matrix.image }}:${{ matrix.image_tag }}

#       - name: Upload artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: python-wheels-${{ runner.os }}-${{ matrix.image }}
#           path: artifacts

#   macos:
#     name: Build Python artifacts on macos
#     runs-on: macos-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: MatteoH2O1999/setup-python@v4
#         with:
#           python-version: '3.7' # Only used to run the build script

#       - name: Run build script (Bash)
#         run: |
#             $PYTHON_EXE -m venv .venv
#             source .venv/bin/activate
#             python -m pip install -U virtualenv
#             bash kokoro/release/python/macos/build_artifacts.sh
#         env:
#             PYTHON_EXE: python3

#       - name: Upload artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: python-wheels-${{ runner.os }}
#           path: artifacts

  windows:
    name: Build Python artifacts on windows
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      # Make sure MSBuild/VS env is on PATH (VS 2022 is preinstalled)
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
      # Try to add the v140 toolset into the preinstalled VS 2022
      - name: Add MSVC v140 component to VS 2022
        shell: pwsh
        run: |
            $url = "https://download.microsoft.com/download/e/e/d/eedf18a8-4aed-4ce0-bebe-70a83094fc5a/BuildTools_Full.exe"
            $exe = "$env:RUNNER_TEMP\vs2015_buildtools.exe"
            Invoke-WebRequest $url -OutFile $exe
            & $exe /Quiet /NoRestart
      - name: Set up Python
        uses: MatteoH2O1999/setup-python@v4
        with:
          python-version: '3.6' # Only used to run the build script
      - name: Run build script (Bash)
        run: |
            python3.exe -m venv .venv
            .venv\Scripts\Activate.ps1
            python.exe -m pip install -U virtualenv
            kokoro/release/python/windows/build_artifacts.bat
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-wheels-${{ runner.os }}
          path: artifacts
